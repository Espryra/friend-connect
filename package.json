{
  "type": "commonjs",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "bedrock-portal": "^2.1.0",
    "bedrock-protocol": "^3.55.0",
    "prismarine-auth": "^3.1.0"
  },
  "scripts": {
    "setup": "clear && echo Please make sure you edited the lib/config.json file! && echo Installing packages... && npm install --silent --no-package-lock && echo Compiling... && npx tsc && echo Done! Now just run \"npm start\"",
    "start": "clear && node dist/main.js"
  }
}
